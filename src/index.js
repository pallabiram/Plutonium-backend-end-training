const bodyParser = require('body-parser')
const express=require('express')
const mongoose=require('mongoose')

const app=express()

app.listen(3000,function(){console.log("code is running sucsessfully")})